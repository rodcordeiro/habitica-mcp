# Runtime

Validation/development commands: pnpm check, pnpm test, pnpm build, pnpm lint by package.json.

Run only the focused command needed for the current change. If validation cannot run, record the reason and strongest inspection evidence.
